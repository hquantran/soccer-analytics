
# ELT transform layer (SQL models run against the DuckDB warehouse post-load).
dbt-core>=1.8.0
dbt-duckdb>=1.8.0

# Extraction layer dependencies for the API-SPORTS pipeline.
dlt[duckdb]>=1.27.2
requests>=2.31.0
certifi>=2024.0.0

# Streamlit dashboard
streamlit>=1.32.0
pandas>=2.0.0
pyarrow>=14.0.0
openpyxl>=3.1.0
plotly>=5.18.0
streamlit-keyup>=0.2.0
